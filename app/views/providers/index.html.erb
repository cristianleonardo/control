<%= render(
  layout: 'shared/a/base',
  :locals => {
    browser_title: 'Proveedores',
    breadcrumbs: {
      crumbs: [],
      active_label: 'Proveedores'
    }
  }) do %>
    <div class="row">
      <div class="col-xs-4 col-lg-6">
        <%= link_to(
          new_provider_path,
          class: 'btn btn-primary'
        ) do %>
          <%= fw_icon('plus') %>
        <% end %>
      </div>
    </div>

  <% if @providers.empty? %>
    <br>
    <div class="alert alert-info">
      <strong>&iexcl;Atenci&oacute;n!</strong>
      Aun no se registran proveedores, has clic en el botón <%= fw_icon('plus') %> para agregar el primero.
    </div>
  <% else %>
    <div class="row">
      <div class="col-lg-12">
        <br>
        <h5>Listado de proveedores registrados</h5>
        <hr>
        <div class="table-responsive">
          <table class="table table-condensed">
            <thead>
              <tr>
                <th width='15%'></th>
                <th width='45%'>Numero</th>
                <th width='45%'>Nombre</th>
              </tr>
            </thead>
            <tbody>
              <% @providers.each do |provider| %>
                <tr>
                  <td>
                    <%= link_to(edit_provider_path(provider), class: "btn btn-default btn-xs") do %>
                      <%= fw_icon('pencil', 'Editar', 'xs') %>
                      <% end %>
                  </td>
                  <td><%= provider.number %></td>
                  <td><%= provider.name %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <%#= paginate @contractor_types, :window => 4 %>
      </div>
    </div>
  <% end -%>
  <% end -%>
