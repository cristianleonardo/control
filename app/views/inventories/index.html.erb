<%= render(
  layout: 'shared/a/base',
  :locals => {
    browser_title: 'Inventario',
    breadcrumbs: {
      crumbs: [],
      active_label: 'Inventario'
    }
  }) do %>
    <div class="row">
      <div class="col-xs-4 col-lg-6">
        <%= link_to(
          new_work_inventory_path,
          class: "btn btn-primary"
        ) do %>
          <%= fw_icon('plus') %>
        <% end %>
      </div>
    </div>

  <% if @inventories.empty? %>
    <br>
    <div class="alert alert-info">
      <strong>&iexcl;Atenci&oacute;n!</strong>
      Esta obra aun no tiene registrado inventarios, has clic en el botón <%= fw_icon('plus') %> para agregar el primero.
    </div>
  <% else %>
    <div class="row">
      <div class="col-lg-12">
        <br>
        <h5>Listado de inventarios registrados</h5>
        <hr>
        <div class="table-responsive">
          <table class="table table-condensed">
            <thead>
              <tr>
                <th width='5%'></th>
                <th width='15%'></th>
                <th width='45%'>Codigo</th>
                <th width='45%'>Estado</th>
              </tr>
            </thead>
            <tbody>
              <% @inventories.each do |inventory| %>
                <tr>
                  <td>
                    <%= link_to(edit_work_inventory_path(@work, inventory), class: "btn btn-default btn-xs") do %>
                      <%= fw_icon('pencil', 'Editar', 'xs') %>
                      <% end %>
                  </td>
                  <td>
                      <%= link_to(work_invenroy_inputs_path(@work, id: inventory.id), class: "btn btn-default btn-xs") do %>
                      <%= fw_icon('pencil', 'Insumos', 'xs') %>
                      <% end %>
                  </td>
                  <td><%= inventory.code %></td>
                  <td><%= inventory.state %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <%#= paginate @workor_types, :window => 4 %>
      </div>
    </div>
  <% end -%>
  <% end -%>
