<%= render(
  layout: 'shared/a/base',
  :locals => {
    browser_title: 'Obras',
    breadcrumbs: {
      crumbs: [],
      active_label: 'Obras'
    }
  }) do %>
    <div class="row">
      <div class="col-xs-4 col-lg-6">
        <%= link_to(
          new_work_path,
          class: 'btn btn-primary'
        ) do %>
          <%= fw_icon('plus') %>
        <% end %>
      </div>
    </div>

  <% if @works.empty? %>
    <br>
    <div class="alert alert-info">
      <strong>&iexcl;Atenci&oacute;n!</strong>
      Aun no se registran obras, has clic en el botón <%= fw_icon('plus') %> para agregar el primero.
    </div>
  <% else %>
    <div class="row">
      <div class="col-lg-12">
        <br>
        <h5>Listado de obras registrados</h5>
        <hr>
        <div class="table-responsive">
          <table class="table table-condensed">
            <thead>
              <tr>
                <th width='5%'></th>
                <th width='15%'></th>
                <th width='15%'>Numero</th>
                <th width='15%'>Presupesto</th>
                <th width='20%'>Tipo</th>
              </tr>
            </thead>
            <tbody>
              <% @works.each do |work| %>
                <tr>
                  <td>
                    <%= link_to(edit_work_path(work), class: "btn btn-default btn-xs") do %>
                      <%= fw_icon('pencil', 'Editar', 'xs') %>
                    <% end %>
                  </td>
                  <td>
                    <%= link_to(work_inventories_path(work), class: "btn btn-default btn-xs") do %>
                      <%= fw_icon('pencil', 'Inventario', 'xs') %>
                    <% end %>
                  </td>
                  <td><%= work.number %></td>
                  <td><%= work.budget %></td>
                  <td><%= work.work_type.name %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
        <%#= paginate @contractor_types, :window => 4 %>
      </div>
    </div>
  <% end -%>
  <% end -%>
